# Dashboard Tour Card V2 Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Refine the dashboard onboarding card so it matches the approved speech-bubble Neubrutalism 3D direction from `tour-card-v2.html` while staying compatible with the live `nextstepjs` integration.

**Architecture:** Keep the change local to the dashboard tour card component and a new focused test file. The implementation should replace the current tooltip-like silhouette with a self-contained bubble shell, tail, and action layout while reusing the existing `Step` data and NextStep runtime contract.

**Tech Stack:** React 19, TypeScript, NextStep.js, Bun test, ESLint

---

### Task 1: Lock the approved speech-bubble shape and control states with failing tests

**Files:**

- Create: `packages/dashboard/src/components/organisms/neub-tour-card.test.tsx`
- Modify: `packages/dashboard/src/components/organisms/neub-tour-card.tsx`

**Step 1: Write the failing test**

Create `packages/dashboard/src/components/organisms/neub-tour-card.test.tsx` with source-level rendering tests using `renderToStaticMarkup`.

Cover these cases:

```tsx
expect(html).toContain('Xin chao')
expect(html).toContain('background-color:#ffd166')
expect(html).toContain('data-tour-tail-outer')
expect(html).toContain('B\u01b0\u1edbc 2 / 3')
```

Add separate expectations for:

- first step: no previous button
- middle step: previous + next visible
- last step: skip hidden and completion button text shown

**Step 2: Run test to verify it fails**

Run: `bun test packages/dashboard/src/components/organisms/neub-tour-card.test.tsx`

Expected: FAIL because the current component has no tail markers and does not yet match the approved control layout.

### Task 2: Implement the speech-bubble shell and tail

**Files:**

- Modify: `packages/dashboard/src/components/organisms/neub-tour-card.tsx`

**Step 1: Write minimal implementation**

Refactor the outer wrapper so the card uses:

- compact speech-bubble shell
- hard neubrutalist border and shadow
- two-layer bubble tail using DOM nodes with stable markers such as:

```tsx
<div data-tour-tail-outer />
<div data-tour-tail-inner />
```

Ignore the visual `arrow` prop from `nextstepjs` so the custom silhouette defines the final look.

**Step 2: Run the focused test**

Run: `bun test packages/dashboard/src/components/organisms/neub-tour-card.test.tsx`

Expected: still FAIL on hierarchy and action-row assertions until those are implemented.

### Task 3: Implement the approved hierarchy, typography, and button states

**Files:**

- Modify: `packages/dashboard/src/components/organisms/neub-tour-card.tsx`

**Step 1: Add the approved card layout**

Implement:

- compact top meta row
- `Bước x / y` badge
- optional skip action for non-final steps
- title/body typography split
- `Prev`, `Next`, and `Hoàn thành` button treatments that match the approved visual language

Keep the background as `step.color` with no gradient on the card body.

**Step 2: Support final-step treatment**

Add the special completion styling branch:

- brighter text
- `Hoàn thành` label
- same family as other cards

**Step 3: Run the focused test**

Run: `bun test packages/dashboard/src/components/organisms/neub-tour-card.test.tsx`

Expected: PASS

### Task 4: Clean up the component contract for runtime stability

**Files:**

- Modify: `packages/dashboard/src/components/organisms/neub-tour-card.tsx`

**Step 1: Remove unused runtime dependencies**

If the `arrow` prop is no longer used visually, remove it from destructuring or explicitly ignore it so lint stays clean.

Keep the internal branching readable:

- first step
- middle step
- final step

Do not introduce unnecessary helpers or global CSS.

**Step 2: Run dashboard typecheck**

Run: `bun run typecheck`

Expected: PASS

### Task 5: Verify lint and focused tests

**Files:**

- Modify: none

**Step 1: Run the focused component test**

Run: `bun test packages/dashboard/src/components/organisms/neub-tour-card.test.tsx`

Expected: PASS

**Step 2: Run repo lint**

Run: `bun run lint`

Expected: PASS

### Task 6: Commit the tour card refinement

**Files:**

- Modify: `packages/dashboard/src/components/organisms/neub-tour-card.tsx`
- Create: `packages/dashboard/src/components/organisms/neub-tour-card.test.tsx`

**Step 1: Create the commit**

Run:

```bash
git add docs/plans/2026-04-02-tour-card-v2-design.md \
  docs/plans/2026-04-02-tour-card-v2.md \
  packages/dashboard/src/components/organisms/neub-tour-card.tsx \
  packages/dashboard/src/components/organisms/neub-tour-card.test.tsx
git commit -m "feat(repo): refine dashboard tour card v2 styling"
```

Expected: commit succeeds after hooks.
