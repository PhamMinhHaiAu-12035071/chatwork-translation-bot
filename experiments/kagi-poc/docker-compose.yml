version: '3.8'

services:
  turnstile-solver:
    image: theyka/turnstile_solver:latest
    container_name: turnstile-solver
    platform: linux/amd64
    ports:
      - '8080:5000' # Changed to 8080 to avoid port conflict
    environment:
      - TZ=Asia/Ho_Chi_Minh
    restart: unless-stopped
